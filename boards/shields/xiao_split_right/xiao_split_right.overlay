/ {
    chosen {
        zmk,kscan = &kscan0;
    };

    kscan0: kscan {
        compatible = "zmk,kscan-gpio-matrix";
        label = "KSCAN";

        row-gpios = < &gpio0 2 GPIO_ACTIVE_LOW >;   /* D0 = P0.02 */
        col-gpios = < &gpio0 3 GPIO_ACTIVE_LOW >;   /* D1 = P0.03 */
    };
};
